<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:padding="20dp"> <!-- 🔥 全局内缩20dp，防切角 -->

    <!-- ========================= -->
    <!-- 🟢 图层 A：准备阶段 (Ready Mode) -->
    <!-- ========================= -->
    <LinearLayout
        android:id="@+id/layoutReady"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="visible"> <!-- 初始可见 -->

        <!-- 巨大的圆形开始按钮 -->
        <!-- 注意：这里其实就是原来的 mainButton，只是我们把它变圆了 -->

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:text="RefLog"
            android:textColor="#666666"
            android:textSize="14sp" />

        <!-- 巨大的圆形开始按钮（纯文字版） -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnBigStart"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:insetLeft="0dp"
            android:insetRight="0dp"
            app:cornerRadius="70dp"
            app:backgroundTint="#00E676"
            android:text="@string/btn_start"
            android:textColor="#000000"
            android:textSize="28sp"
            android:textStyle="bold"
            android:gravity="center"
            android:padding="0dp"
            android:layout_marginBottom="16dp"/>

        <ImageButton
            android:id="@+id/btnHistorySmall"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/card_background"
            android:src="@drawable/ic_history"
            app:tint="#888888"
            android:contentDescription="History" />

    </LinearLayout>


    <!-- ========================= -->
    <!-- 🔴 图层 B：比赛阶段 (Running Mode) -->
    <!-- ========================= -->
    <RelativeLayout
        android:id="@+id/layoutRunning"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <View
            android:id="@+id/touchOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent" />

        <!-- 1. 核心计时区 (绝对居中) -->
        <LinearLayout
            android:id="@+id/timerContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:layout_centerInParent="true">

            <TextView
                android:id="@+id/statusLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/status_first_half"
                android:textColor="#4CAF50"
                android:textSize="16sp"
                android:textStyle="bold"
                android:layout_marginBottom="4dp"/>

            <TextView
                android:id="@+id/mainTimeLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="00:00"
                android:textColor="#4CAF50"
                android:textSize="64sp"
                android:fontFamily="monospace"
                android:includeFontPadding="false"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/stoppageTimeLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="00:00"
                android:textSize="20sp"
                android:textColor="#888888"
                android:background="@drawable/bg_stoppage_capsule"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp" />
        </LinearLayout>

        <!-- 2. 控制面板 (初始藏在底部) -->
        <LinearLayout
            android:id="@+id/controlPanel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:gravity="center"
            android:padding="8dp"
            android:background="#CC000000"
            android:translationY="200dp">

            <!-- 原来的 mainButton (但在比赛中变成了暂停键) -->
            <!-- 这里我们需要用一个新的 ID 或者是复用逻辑，但为了简单，我们放两个新按钮在这里，代码里绑定事件 -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnPauseRound"
                android:layout_width="60dp"
                android:layout_height="60dp"
                app:cornerRadius="30dp"
                app:backgroundTint="#D32F2F"
                app:icon="@drawable/pause_circle"
                app:iconGravity="textStart"
                app:iconPadding="0dp"
                android:layout_marginEnd="16dp" />

            <!-- 结束按钮 -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnEndRound"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:background="@drawable/bg_end_button_progress"
                app:backgroundTint="@null"
                app:icon="@drawable/stop_circle"
                app:iconGravity="textStart"
                app:iconPadding="0dp" />

        </LinearLayout>

    </RelativeLayout>

    <!-- ⚠️ 兼容性保留：为了防止 MainActivity 报错找不到 ID -->
    <!-- 我们在这里把原来代码里用到的一些 ID 藏起来，避免报错，虽然它们在圆屏模式下可能没用 -->
    <!-- ⚠️ 修正后的占位符：类型必须是 MaterialButton，防止 ClassCastException -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/mainButton"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/endHalfButton"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnHistory"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"/>

</RelativeLayout>